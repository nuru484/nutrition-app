<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link href="styles/tailwindOut.css" rel="stylesheet" />
    <title>Meals Directory</title>
    <style>
      main {
        padding: 2rem;
      }

      .meals-container {
        padding-bottom: 2rem;
      }

      .meal-card {
        transition: transform 0.3s ease, box-shadow 0.3s ease;
        height: 100%;
      }

      .meal-card:hover {
        transform: translateY(-4px);
        box-shadow: 0 20px 25px -5px rgba(0, 0, 0, 0.1), 0 10px 10px -5px rgba(0, 0, 0, 0.04);
      }

      .meal-image {
        height: 200px;
        object-fit: cover;
      }

      .nutrient-badge {
        background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
        color: white;
        padding: 0.25rem 0.75rem;
        border-radius: 9999px;
        font-size: 0.875rem;
        font-weight: 500;
      }

      .description-text {
        display: -webkit-box;
        -webkit-line-clamp: 3;
        -webkit-box-orient: vertical;
        overflow: hidden;
        line-height: 1.5;
      }
    </style>
  </head>
  <body class="bg-gradient-to-br from-blue-50 via-white to-indigo-100 min-h-screen">
    <main class="container mx-auto">
      <!-- Header Section -->
      <div class="text-center mb-12">
        <h1 class="text-5xl font-bold bg-gradient-to-r from-blue-600 to-indigo-600 bg-clip-text text-transparent mb-4">
          Meals Directory
        </h1>
        <p class="text-xl text-gray-600 max-w-3xl mx-auto leading-relaxed">
          Explore our comprehensive collection of nutritious meals with detailed nutritional information and descriptions.
        </p>
      </div>

      <!-- Meals Grid -->
      <div class="meals-container">
        <% if (meals.length > 0) { %>
          <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 gap-8">
            <% meals.forEach(meal => { %>
              <div class="meal-card bg-white rounded-2xl shadow-sm border border-gray-100 overflow-hidden">
                <!-- Meal Image -->
                <div class="relative">
                  <img
                    src="/photos/meals/<%= meal.photo %>"
                    alt="<%= meal.name %>"
                    class="meal-image w-full"
                    onerror="this.src='/photos/meals/placeholder.jpg'"
                  />
                  <div class="absolute top-3 right-3">
                    <span class="nutrient-badge">
                      <%= meal.calories %> kcal
                    </span>
                  </div>
                </div>

                <!-- Meal Name -->
                <div class="p-6">
                  <h2 class="text-xl font-bold text-gray-900 mb-3 line-clamp-2">
                    <%= meal.name %>
                  </h2>

                  <!-- Description -->
                  <% if (meal.description && meal.description.trim() !== '') { %>
                    <p class="description-text text-gray-600 text-sm mb-4 leading-relaxed">
                      <%= meal.description %>
                    </p>
                  <% } %>

                  <!-- Nutritional Information -->
                  <div class="grid grid-cols-2 gap-3 mb-6">
                    <div class="text-center p-3 bg-gradient-to-br from-green-50 to-green-100 rounded-xl">
                      <div class="text-2xl font-bold text-green-600"><%= meal.protein %></div>
                      <div class="text-xs text-green-700 font-medium uppercase tracking-wide">Protein (g)</div>
                    </div>
                    <div class="text-center p-3 bg-gradient-to-br from-blue-50 to-blue-100 rounded-xl">
                      <div class="text-2xl font-bold text-blue-600"><%= meal.carbohydrates %></div>
                      <div class="text-xs text-blue-700 font-medium uppercase tracking-wide">Carbs (g)</div>
                    </div>
                  </div>

                  <div class="grid grid-cols-2 gap-3">
                    <div class="text-center p-3 bg-gradient-to-br from-yellow-50 to-yellow-100 rounded-xl">
                      <div class="text-2xl font-bold text-yellow-600"><%= meal.fat %></div>
                      <div class="text-xs text-yellow-700 font-medium uppercase tracking-wide">Fat (g)</div>
                    </div>
                    <div class="text-center p-3 bg-gradient-to-br from-purple-50 to-purple-100 rounded-xl col-span-2 sm:col-span-1">
                      <div class="text-lg font-bold text-purple-600">
                        <span class="text-2xl"><%= meal.protein + meal.carbohydrates + meal.fat %></span>g total
                      </div>
                      <div class="text-xs text-purple-700 font-medium uppercase tracking-wide">Macros</div>
                    </div>
                  </div>
                </div>
              </div>
            <% }) %>
          </div>

          <!-- Stats Row -->
          <div class="mt-12 grid grid-cols-1 md:grid-cols-4 gap-6">
            <div class="bg-white rounded-2xl p-6 text-center shadow-sm border border-gray-100">
              <div class="text-3xl font-bold text-blue-600"><%= meals.length %></div>
              <div class="text-sm text-gray-600 font-medium uppercase tracking-wide">Total Meals</div>
            </div>
           
          </div>
        <% } else { %>
          <div class="text-center py-20">
            <div class="mx-auto w-24 h-24 bg-gray-100 rounded-full flex items-center justify-center mb-6">
              <svg class="w-12 h-12 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1" d="M20.618 5.382A12.1 12.1 0 0112 3a11.999 11.999 0 00-8 3.382 12 12 0 000 17.236 12.1 12.1 0 008 3.382 11.999 11.999 0 008-3.382 12 12 0 000-17.236zm-2.093 14.823a10 10 0 11-2.588-3.46l-3.92-3.92a2 2 0 01-.293-.59l-.668-3.346a2 2 0 012.122-2.122l3.346.668a2 2 0 01.59.293l3.92 3.92a10 10 0 01-3.589 2.637z"></path>
              </svg>
            </div>
            <h2 class="text-2xl font-semibold text-gray-900 mb-2">No Meals Available</h2>
            <p class="text-gray-500 max-w-md mx-auto">
              Our meals directory is currently empty. Please check back later for delicious and nutritious meal options.
            </p>
          </div>
        <% } %>
      </div>
    </main>
  </body>
</html>